[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: marks tests that require a live Outlook COM connection (run manually)

# Default: exclude integration tests
addopts = -v --tb=short -m "not integration"
