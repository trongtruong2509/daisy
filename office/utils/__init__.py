"""
Shared utilities for the office/ COM automation layer.

Modules:
    com      — COM bootstrapping (CoInitialize, Dispatch, availability detection)
    helpers  — Generic COM application lifecycle (get-or-create + safe-quit pattern)
"""
