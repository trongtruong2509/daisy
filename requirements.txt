# Office Automation Foundation - Python Dependencies
#
# Install with: pip install -r requirements.txt
#
# Note: This project is Windows-only due to Outlook COM requirements.

# Core dependencies
pywin32>=306          # Windows COM automation (required for Outlook)
python-dotenv>=1.0.0  # Environment variable loading

# Data processing
openpyxl>=3.1.2       # Excel file handling (.xlsx read/write)
xlrd>=2.0.1           # Excel file handling (.xls read)
pandas>=2.0.0         # Data manipulation (optional but recommended)

# PDF
pikepdf>=8.0.0        # PDF password protection

# Colors and Terminal
colorama>=0.4.6       # Cross-platform colored terminal output (Windows, Mac, Linux)

# Parsing
beautifulsoup4>=4.12.0  # HTML parsing
PyYAML>=6.0.1           # YAML config support (optional)

# Development
pytest>=7.4.0
pytest-cov>=4.1.0
ruff>=0.8.0           # Fast Python linter & formatter
